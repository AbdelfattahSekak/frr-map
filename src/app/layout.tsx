import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "FRR Map",
  description:
    "An open-source initiative designed to support the revitalization of rural France through an interactive map showcasing regions undergoing efforts to rejuvenate their local economies, infrastructure, and community services.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
